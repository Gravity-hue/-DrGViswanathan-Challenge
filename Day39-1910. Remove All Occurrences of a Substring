class Solution {
public:
    string removeOccurrences(string s, string part) {
         while(s.length()>0 && s.find(part)<s.length()){//There exists a string && it finds a valid index
            s.erase(s.find(part),part.length());
        }
        return s;
    }
};
